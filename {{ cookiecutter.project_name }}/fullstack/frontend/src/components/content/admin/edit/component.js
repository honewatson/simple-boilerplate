import { default as editComponent } from '../../../crud/edit/component';

import { default as componentConfig } from '../../config';

const ID = "main__content";

const component = editComponent(ID, componentConfig);

export default component;



