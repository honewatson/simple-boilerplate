import { default as newComponent } from '../../../crud/new/component';

import { default as componentConfig } from '../../config';

const ID = "main__content";

const component = newComponent(ID, componentConfig);

export default component;



