import { default as listComponent } from '../../crud/frontList/component';

import { default as componentConfig } from '../config';

const ID = "main__content";

const component = listComponent(ID, componentConfig);

export default component;