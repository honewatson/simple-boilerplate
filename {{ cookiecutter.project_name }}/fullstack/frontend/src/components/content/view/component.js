import { default as viewComponent } from '../../crud/frontView/component';

import { default as componentConfig } from '../config';

const ID = "main__content";

const component = viewComponent(ID, componentConfig);

export default component;